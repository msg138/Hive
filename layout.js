Lemonade.include("ui.js");

// This will just organize the user layout based on the menu provided.
function Layout(menu){
  if(menu=="main")
  {
  }else if(menu == "game")
  {
    var m = {};
    
    return m;
  }
  
  return undefined;
}